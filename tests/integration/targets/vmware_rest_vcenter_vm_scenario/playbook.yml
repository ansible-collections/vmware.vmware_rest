- hosts: localhost
  gather_facts: no

  tasks:
    - name: Import vmware_rest_vcenter_vm_scenario test
      ansible.builtin.import_role:
        name: vmware_rest_vcenter_vm_scenario
      tags:
        - eco-vcenter-ci
