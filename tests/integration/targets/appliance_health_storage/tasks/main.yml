---
- name: Test appliance_health_storage_info module against MockServer
  environment: "{{ environment_auth_vars }}"
  block:
    - name: Load API spec expectations for get storage health
      ansible.builtin.uri:
        url: http://localhost:1080/mockserver/openapi
        method: PUT
        status_code: [200, 201]
        body_format: json
        body:
          specUrlOrPayload: "file:/mockserver_specs/default.json"
          operationsAndResponses:
            get: "200"

    - name: Get the storage health status
      vmware.vmware_rest.appliance_health_storage_info:
      register: get_storage_health_info

    - name: Assert storage health status
      ansible.builtin.assert:
        that:
          - get_storage_health_info.value is defined
          - get_storage_health_info.value == 'green'
