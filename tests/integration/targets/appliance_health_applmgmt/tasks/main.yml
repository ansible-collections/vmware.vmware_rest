---
- name: Test appliance_health_applmgmt_info module against MockServer
  environment: "{{ environment_auth_vars }}"
  block:
    - name: Load API spec expectations for get health status
      ansible.builtin.uri:
        url: http://localhost:1080/mockserver/openapi
        method: PUT
        status_code: [200, 201]
        body_format: json
        body:
          specUrlOrPayload: "file:/mockserver_specs/default.json"
          operationsAndResponses:
            get: "200"

    - name: Get the health state of applmgmt
      vmware.vmware_rest.appliance_health_applmgmt_info:
      register: get_applmgmt_health_info

    - name: Assert applmgmt health status
      ansible.builtin.assert:
        that:
          - get_applmgmt_health_info.value is defined
          - get_applmgmt_health_info.value == 'green'
