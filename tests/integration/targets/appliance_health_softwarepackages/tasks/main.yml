---
- name: Test appliance_health_softwarepackages_info module against MockServer
  environment: "{{ environment_auth_vars }}"
  block:
    - name: Load API spec expectations for get software packages health
      ansible.builtin.uri:
        url: http://localhost:1080/mockserver/openapi
        method: PUT
        status_code: [200, 201]
        body_format: json
        body:
          specUrlOrPayload: "file:/mockserver_specs/default.json"
          operationsAndResponses:
            get: "200"

    - name: Get the software packages health status
      vmware.vmware_rest.appliance_health_softwarepackages_info:
      register: get_softwarepackages_health_info

    - name: Assert software packages health status
      ansible.builtin.assert:
        that:
          - get_softwarepackages_health_info.value is defined
          - get_softwarepackages_health_info.value == 'green'
