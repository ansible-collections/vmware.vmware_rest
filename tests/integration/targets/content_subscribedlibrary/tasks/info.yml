---
# ============================================================
# CONTENT_SUBSCRIBEDLIBRARY_INFO TESTS
# ============================================================
# Read-only info module backed by the /content/subscribed-library LIST and
# /content/subscribed-library/{library_id} GET endpoints. Results are always
# returned as both `info` (list[dict]) and `value`.

# Test 1: List subscribed libraries when none exist
- name: List subscribed libraries (empty)
  vmware.vmware_rest.content_subscribedlibrary_info: {}
  register: list_empty

- name: Assert empty list
  ansible.builtin.assert:
    that:
      - list_empty.info is defined
      - list_empty.info | length == 0
      - not list_empty.changed

# Test 2: Get a single subscribed library by ID
- name: Reset before get expectations
  ansible.builtin.include_role:
    name: prepare_simulator
  vars:
    prepare_simulator_reset: true

- name: Load API spec expectations for get
  ansible.builtin.uri:
    url: "{{ mockserver_url }}/mockserver/openapi"
    method: PUT
    status_code: [200, 201]
    body_format: json
    body:
      specUrlOrPayload: "file:/mockserver_specs/default.json"
      operationsAndResponses:
        Content.SubscribedLibrary_get: "200"

- name: Get a subscribed library by ID
  vmware.vmware_rest.content_subscribedlibrary_info:
    library_id: 3393956a-43ed-4e7f-bd96-eb39bd604445
  register: get_by_id

- name: Assert single library details
  ansible.builtin.assert:
    that:
      - get_by_id.value is defined
      - get_by_id.value.name == 'my_subscribed_library'
      - get_by_id.value.type == 'SUBSCRIBED'
      - get_by_id.value.subscription_info.automatic_sync_enabled
      - get_by_id.info is defined
      - get_by_id.info | length == 1
      - get_by_id.info[0].name == 'my_subscribed_library'
      - not get_by_id.changed

# Test 3: List multiple subscribed libraries
- name: Reset before multiple list expectations
  ansible.builtin.include_role:
    name: prepare_simulator
  vars:
    prepare_simulator_reset: true

- name: Load API spec expectations for multiple
  ansible.builtin.uri:
    url: "{{ mockserver_url }}/mockserver/openapi"
    method: PUT
    status_code: [200, 201]
    body_format: json
    body:
      specUrlOrPayload: "file:/mockserver_specs/list_multiple.json"
      operationsAndResponses:
        Content.SubscribedLibrary_list: "200"
        Content.SubscribedLibrary_get: "200"

- name: List subscribed libraries (multiple)
  vmware.vmware_rest.content_subscribedlibrary_info: {}
  register: list_multiple

- name: Assert multiple libraries listed
  ansible.builtin.assert:
    that:
      - list_multiple.value is defined
      - list_multiple.value | length == 2
      - list_multiple.info is defined
      - list_multiple.info | length == 2
      - not list_multiple.changed

# Test 4: Check mode returns the same read-only result without error
- name: Get a subscribed library by ID (check mode)
  vmware.vmware_rest.content_subscribedlibrary_info:
    library_id: 3393956a-43ed-4e7f-bd96-eb39bd604445
  check_mode: true
  register: get_check

- name: Assert check mode reports no change for read-only info module
  ansible.builtin.assert:
    that:
      - get_check.value is defined
      - not get_check.changed
